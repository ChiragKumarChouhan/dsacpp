#include<iostream>
#include<vector>

//function for dutch national flag algo

using namespace std;
int dutch(vector<int> &arr,int n){
int mid=0,high-n-1,low=0;
while(mid<=high){
    if(a[mid]==0){
        swap(a[low],a[mid])
        mid++,low++;
    }else if(a[mid==1]){
        mid++;
    }else
    swap(a[high],a[mid]);
    high--;
}
}
